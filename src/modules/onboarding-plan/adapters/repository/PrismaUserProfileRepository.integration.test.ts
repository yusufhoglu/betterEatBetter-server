import { PrismaUserProfileRepository } from './PrismaUserProfileRepository';

describe('PrismaUserProfileRepository', () => {
  test.todo('PrismaUserProfileRepository: Testcontainers ile gercek Postgres/Redis uzerinde');
});
