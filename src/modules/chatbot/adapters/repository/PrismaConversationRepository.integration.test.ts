import { PrismaConversationRepository } from './PrismaConversationRepository';

describe('PrismaConversationRepository', () => {
  test.todo('PrismaConversationRepository: Testcontainers ile gercek Postgres/Redis uzerinde');
});
