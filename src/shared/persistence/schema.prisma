// TODO: modeller modul rule dosyalarina gore eklenecek. pgvector extension RAG embedding icin kullanilacak.
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider   = "postgresql"
  url        = env("DATABASE_URL")
  extensions = [vector]
}
